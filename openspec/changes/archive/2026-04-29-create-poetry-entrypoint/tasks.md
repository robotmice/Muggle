## 1. Code Changes

- [x] 1.1 Add `run()` function to `muggle/app.py` that invokes `app.run()`.
- [x] 1.2 Update `pyproject.toml` to include the `muggle` script in `[tool.poetry.scripts]`.

## 2. Verification

- [x] 2.1 Verify the new entrypoint by executing `poetry run muggle`.
- [x] 2.2 Confirm backward compatibility by running `python muggle/app.py`.
